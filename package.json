{
  "name": "deploy",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "prebuild": "rm -rf ./server/public",
    "postinstall": "cd client && npm install",
    "build": "npm run build",
    "start": "cd ../server && npm start"
  }
}
